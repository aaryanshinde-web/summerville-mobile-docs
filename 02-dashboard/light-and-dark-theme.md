# Light & Dark Theme

_Summerville Mobile › Dashboard › Light & Dark Theme_

## Dashboard: Light & Dark Theme

> A member-controlled visual preference — Light, Dark, or System — stored on the device. The Side Menu lists **Dark Theme** as the entry point; picking Dark flips every surface in the app to a dark palette.

**How to get here:** Side Menu (☰ top-right) → **Dark Theme** (near the bottom)

### Step-by-Step Workflow

#### Step 1: Open Side Menu → Dark Theme

Tap the hamburger icon to open the Side Menu, then scroll down past the main navigation rows to find **Dark Theme — Enable or Disable Dark Theme**. Tap the row to open the theme picker.

![Step 1: Open Side Menu → Dark Theme](../.gitbook/assets/Screenshot_1776963763.png)

#### Step 2: Pick Theme Mode (Light / Dark / System)

The theme picker offers three modes: **System** (follows your iOS or Android theme automatically), **Light** (always-light), or **Dark** (always-dark). Your selection applies immediately — no app restart needed. Brand colors (navy header, signature Summerville blue accents) are preserved across both palettes so the identity stays consistent.

*Note: neither the picker destination screen nor a dark-mode-active Dashboard screenshot is in this capture set. If Dark Theme is active in your build, capturing the picker and a dark Dashboard will let us swap the placeholder image below for the real screen.*

![Step 2: Pick Theme Mode — TODO: capture theme picker + dark-mode Dashboard](TODO-theme-picker.png)

### Summary

Theme is a local preference — the choice on your phone doesn't sync to tablet or web, and reinstalling the app resets to **System**. Respecting the OS-level theme is the right default for members who run system-wide dark mode at night; explicit Light or Dark overrides the system for members who prefer a fixed appearance. Because it's the only truly visual preference in the app, it lives at the bottom of the Side Menu rather than buried in Settings.

### Key Use Cases

* Member on system-wide dark mode: no action needed — the app picks up dark automatically on first launch.
* Accessibility preference for high contrast: set Dark explicitly to pin the theme regardless of OS setting.
* Member switches phones: the new device defaults to System; re-pin only if the previous device used Light or Dark explicitly.
